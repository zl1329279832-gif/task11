package com.clinic.appointment.model.enums;

public enum AuditAction {
    BOOK,
    CANCEL,
    RESCHEDULE,
    CHECK_IN,
    PASS,
    COMPLETE,
    WAITLIST_JOIN,
    WAITLIST_CONVERT,
    WAITLIST_CANCEL,
    WAITLIST_EXPIRE,
    SUSPEND,
    MIGRATE,
    EXTRA_SLOT
}
